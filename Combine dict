from collections import Counter

item_list = [{'item': 'item1', 'amount': 400}, {'item': 'item2', 'amount': 300}, {'item': 'item1', 'amount': 750}]

result = Counter()

for i in item_list:
    result[i['item']] = result[i['item']] + i['amount']

print(dict(result))

